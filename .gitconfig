# user.name, user.emailなどユーザ（端末）固有情報が入るため
# シンボリックリンクでの利用非推奨。

[alias]
        st = status
        co = checkout
        br = branch
        graph = log --graph --oneline --decorate=full
[core]
        editor = /usr/local/bin/vim
        excludesfile = ~/.gitignore_global

